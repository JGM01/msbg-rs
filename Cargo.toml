[package]
name = "msbg-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
rayon = "1.12.0"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }

[[bench]]
name = "allocator_benches"
harness = false
